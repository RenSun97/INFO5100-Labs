We can use Floyd algorithm to filter the shortest path. By simplifying the city map into an undirected graph, and using inf to replace the directed one, we can get the shortest path in the distance matrix and locate the car position